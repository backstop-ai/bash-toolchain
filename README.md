# bash-toolchain

Backstop mechanism pack for discovering Bash test declarations and executing a
repository's canonical Bash verifier.

The pack intentionally contains no coding-standard or substantiveness rules.

## Development

This repository requires Backstop CLI v0.3.0.

```sh
backstop pack test
backstop gate --all
```
